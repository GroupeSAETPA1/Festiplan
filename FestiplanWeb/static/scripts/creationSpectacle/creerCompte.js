// quand on clique qur le plus on affiche creationCompte
/* TODO:
SELECTION.on('click', '.fa-plus', function() {
    console.log('click')
    let index = $(this).parent().parent().find('.delete').data('index');
    let inter = interList[index];

    $(".creationCompte").css({
        display: "block",
    });
});

SELECTION_HORS_SCENE.on('click', '.fa-plus', function() {
    console.log('click')
    let index = $(this).parent().parent().find('.delete').data('index');
    let inter = interListHorsScene[index];

    $(".creationCompte").css({
        display: "block",
    });
});
*/
