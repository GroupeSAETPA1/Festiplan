<?php

function ($email, $nom, $prenom, $pwd, $login, $pdo) {
    // inscription d'un utilisateur
    // Renvoie vrai ou faux en fonction si l'utilisateur a été inscrit.

    try {
        if ()
    }
}